{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "chmod +x artisan && php artisan serve --host=0.0.0.0 --port=$PORT",
    "restartPolicyType": "on_failure",
    "restartPolicyMaxRetries": 5
  }
}
